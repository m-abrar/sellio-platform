## Completed — 2026-07-04

- [x] **Cross-vertical home feed.** `Page.tsx` fetched only `api.getProducts()` despite being a "unifieds" (multi-vertical) theme — the "Heavyweight Listings" section, and any card linking to a non-product listing, was broken by design. Rewired to `fetchAllVerticals()` from `unifieds/shared/multiVertical.ts` (the same shared infra `unifieds_minimal`/`unifieds_marketplace` use), so the home page now shows a genuine mix of products/properties/autos/jobs/services/events/classifieds with a vertical badge per card and the correct `href` per vertical.
- [x] **Real stats, not fabricated ones.** `HeavyweightGrid` hardcoded "840TB Monthly Data Volume", "12k+ Active Seller Accounts", "1.4M Verified Product Records" — meaningless marketing puffery that would look obviously fake against a real demo install (~30-50 listings per vertical). Replaced with `listingsTotal` / `categoriesCount` / `verticalsCount` props sourced from the same `fetchAllVerticals()` call already running on the home page (no extra request).
- [x] **Multi-vertical detail page.** `ProductPage.tsx` only ever called `api.getProductBySlug()`, so any non-product listing (property/auto/job/service/event/classified) linked from Explore or Home would 404 or show wrong data. Rewired to `fetchVerticalDetail(vertical, slug)`, accepting the `vertical` prop that `MarketplaceDetailRoute` already passes for `/properties/[slug]`, `/autos/[slug]`, etc. Non-product listings get a themed action link (`detail.actionLabel`/`actionHref`) instead of "Add to cart"; product listings keep the real add-to-cart flow. Removed dev-jargon ("MEGA_UNAVAILABLE", "MEGA_{id}" badge, raw slug shown as "Record") in favor of `detail.label`/`detail.kicker`/`detail.meta`.
- [x] **Shared `UnifiedExplorePage` upgraded to multi-vertical** (benefits `unifieds_mega` plus the 4 sibling themes that re-export it as-is: `classic`, `interactive`, `modern`, `standard`). Was products-only with stale jargon (`CATALOG_DIRECTORY`, `REGISTRY_OFFLINE`, `LISTING_{id}`, `EMPTY_RESULTS`). Now uses `fetchAllVerticals()` with vertical filter chips (real per-vertical counts), search, category filter, sort, and "Load more" pagination — same pattern as `unifieds_minimal`'s dedicated Explore page, generalized with the shared `uni-*` CSS classes in `subpages.css` (added `.uni-vertical-chips`/`.uni-vertical-chip`/`.uni-card-vertical-badge`).
- [x] **Non-CMS-driven footer copyright.** `AuthorityFooter` hardcoded `© 2026 Sellio. All rights reserved.` — now `useThemeContent('site_name', 'Sellio')` + `new Date().getFullYear()`, matching the pattern already used elsewhere in the codebase.
- [x] Synced `ThemeSeeder.php`'s `unifieds_mega` section defaults (`collection.description`, `empty.description`) with the updated code fallbacks.
- [x] Verified: `npm run lint` (0 errors, pre-existing unrelated warnings only), `npm run build` (compiles clean, all routes generate), and live browser verification via Playwright against a production build — confirmed real cross-vertical listings + real stats on the home page, working vertical filter chips with correct counts (Products 50, Properties 30, Autos 30, Services 24, Jobs 15, Events 15, Classifieds 50) and filtering, and a property detail page rendering the correct badge/action button/meta fields.

### Notes for a future pass
- Non-product verticals get a single CTA link (e.g. "Browse properties") rather than a real inquiry/apply/booking form — this matches the already-shipped `unifieds_marketplace` pattern, not the fuller `InteractionForms.tsx` flow built for `unifieds_minimal`. Worth revisiting if this theme needs feature parity with minimal.
- `MassiveSyncBar`'s ticker copy ("★ PLATFORM STATUS: ONLINE // 99.99% UPTIME") and the brand wordmark "MEGAGRID" were left as-is — stylistic brand identity / industrial theming, not the dev-jargon class of bug fixed elsewhere.
