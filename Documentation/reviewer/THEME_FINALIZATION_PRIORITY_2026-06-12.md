# Theme Finalization Priority - 2026-06-12

## Purpose

This list ranks Sellio's 52 storefront themes in the recommended order for final polishing before CodeCanyon submission.

The order is based on expected marketplace selling impact, not literal theme-by-theme sales, because the themes are bundled inside Sellio rather than sold as separate CodeCanyon items.

## Market Signals Used

- Ecommerce / multivendor products show the broadest CodeCanyon demand.
- Real estate / property products show strong Laravel-specific demand.
- Job portal and classifieds products have proven demand, but less breadth than ecommerce and real estate.
- Autos, events, and services add differentiation and demo breadth, but should be finalized after the highest-demand verticals unless a specific theme is already nearly perfect.
- Unified marketplace themes matter because they communicate Sellio's overall platform value in the first demo impression.

## Finalization Order

| Rank | Theme Key | Vertical | Priority | Reason |
| ---: | --- | --- | --- | --- |
| 1 | `unifieds_marketplace` | Unified | P0 | **Polished 2026-06-12.** Best overall CodeCanyon positioning: shows Sellio as a broad marketplace platform. |
| 2 | `ecommerce_default` | Ecommerce | P0 | **Polished 2026-06-12.** Core ecommerce buyer expectation; now reads as a complete retail storefront. |
| 3 | `ecommerce_fashion` | Ecommerce | P0 | **Polished 2026-06-13.** Fashion ecommerce is highly visual and strong for screenshots. |
| 4 | `ecommerce_electronics` | Ecommerce | P0 | **Polished 2026-06-13.** Electronics is a common marketplace/demo category with strong commercial appeal. |
| 5 | `properties_modern` | Properties | P0 | **Polished 2026-06-13.** Real estate Laravel scripts have strong proven demand; modern should be the safest showcase. |
| 6 | `properties_luxury` | Properties | P0 | **Polished 2026-06-13.** High visual impact for listing screenshots and demo browsing. |
| 7 | `properties_rental` | Properties | P0 | **Polished 2026-06-13.** Rental/booking angle adds monetizable depth beyond simple listings. |
| 8 | `properties_map` | Properties | P0 | **Polished 2026-06-13.** Map-first real estate search is a major buyer expectation. |
| 9 | `unifieds_default` | Unified | P0 | **Polished 2026-06-13.** Fallback/default theme must be polished because it is the first safety net. |
| 10 | `unifieds_modern` | Unified | P0 | **Polished 2026-06-13.** Strong general-purpose demo candidate. |
| 11 | `classifieds_modern` | Classifieds | P1 | **Polished 2026-06-13.** Classified marketplace is a close fit for CodeCanyon buyers. |
| 12 | `classifieds_general` | Classifieds | P1 | **Polished 2026-06-13.** Baseline classified ads flow should be stable and familiar. |
| 13 | `classifieds_premium` | Classifieds | P1 | **Polished 2026-06-13.** Adds a higher-end classified demo option. |
| 14 | `classifieds_local` | Classifieds | P1 | **Polished 2026-06-13.** Local marketplace positioning is practical and easy for buyers to understand. |
| 15 | `jobs_modern` | Jobs | P1 | **Polished 2026-06-13.** Job board products have proven demand; modern should be the primary jobs showcase. |
| 16 | `jobs_corporate` | Jobs | P1 | **Polished 2026-06-13.** Corporate recruiting is the safest commercial jobs angle. |
| 17 | `jobs_tech` | Jobs | P1 | **Polished 2026-06-13.** Tech hiring has strong visual and business relevance. |
| 18 | `services_marketplace` | Services | P1 | **Polished 2026-06-13.** Service marketplace fits Sellio's multi-vertical promise. |
| 19 | `services_corporate` | Services | P1 | **Polished 2026-06-13.** Practical for agencies and B2B buyers. |
| 20 | `autos_modern` | Autos | P1 | **Polished 2026-06-13.** Modern vehicle marketplace should be the primary autos showcase. |
| 21 | `autos_used` | Autos | P1 | **Polished 2026-06-13.** Used-car marketplace is practical and broadly understandable. |
| 22 | `events_classic` | Events | P1 | **Polished 2026-06-13.** Baseline event listing/booking should be stable. |
| 23 | `events_corporate` | Events | P1 | **Polished 2026-06-13.** Corporate events are commercially useful and less niche than music/festival. |
| 24 | `properties_classic` | Properties | P2 | **Polished 2026-06-13.** Familiar real estate layout; useful but less exciting than modern/luxury/map. |
| 25 | `properties_vacation` | Properties | P2 | **Polished 2026-06-13.** Good visual appeal; secondary after rental/map. |
| 26 | `properties_commercial` | Properties | P2 | **Polished 2026-06-13.** Useful business niche; less broad than residential/rental. |
| 27 | `properties_investment` | Properties | P2 | **Polished 2026-06-13.** Strong niche for investors; polish after core property themes. |
| 28 | `properties_showcase` | Properties | P2 | **Polished 2026-06-13.** Screenshot-friendly but less workflow-heavy. |
| 29 | `properties_platinum` | Properties | P2 | **Polished 2026-06-13.** Premium variant; useful once luxury is solid. |
| 30 | `properties_urban` | Properties | P2 | **Polished 2026-06-13.** Nice lifestyle variant; secondary priority. |
| 31 | `properties_neighborhood` | Properties | P2 | **Polished 2026-06-13.** Useful differentiation, but not a primary sales driver. |
| 32 | `properties_unified` | Properties | P2 | **Polished 2026-06-13.** Good platform bridge; after stronger property demos. |
| 33 | `ecommerce_luxury` | Ecommerce | P2 | **Polished 2026-06-13.** High visual value, but narrower than default/fashion/electronics. |
| 34 | `unifieds_mega` | Unified | P2 | **Polished 2026-06-13.** Strong for mega marketplace feel; check complexity carefully. |
| 35 | `unifieds_standard` | Unified | P2 | **Polished 2026-06-13.** Practical general variant. |
| 36 | `unifieds_classic` | Unified | P2 | **Polished 2026-06-13.** Useful fallback style, lower visual differentiation. |
| 37 | `unifieds_interactive` | Unified | P2 | **Polished 2026-06-13.** Differentiator; polish after core reliability themes. |
| 38 | `unifieds_minimal` | Unified | P2 | **Polished 2026-06-13.** Nice aesthetic variant, but less sales-critical. |
| 39 | `classifieds_deals` | Classifieds | P2 | **Polished 2026-06-13.** Good for bargain marketplace demos. |
| 40 | `classifieds_elite` | Classifieds | P2 | **Polished 2026-06-13.** Premium variant; less essential than modern/general/premium/local. |
| 41 | `jobs_startup` | Jobs | P2 | **Polished 2026-06-13.** Appealing niche, but after modern/corporate/tech. |
| 42 | `jobs_freelance` | Jobs | P2 | **Polished 2026-06-13.** Useful gig-work angle; secondary. |
| 43 | `jobs_blue_collar` | Jobs | P2 | **Polished 2026-06-13.** Removed `useRouter`/`goToExplore`; all buttons → `<a>` links; fixed footer logo `href="#"` → `themeLink('/')`; copyright `TradesWork Platform. All Rights Reserved.` → `TradesWork. All rights reserved.`; empty state "Empty Job Registry"/"backend"/"hydrate" → clean. Lint 0 errors, HTTP 200. |
| 44 | `services_local` | Services | P2 | **Polished 2026-06-13.** Fixed empty state kicker "Empty Service Registry"/"backend"/"hydrate" → clean; header logo `themeLink('')` → `themeLink('/')`; footer logo `<div>` → `<a href={themeLink('/')}>` (arrow→block body); copyright simplified to `HomeFix`. Lint 0 errors, HTTP 200. |
| 45 | `services_creative` | Services | P2 | **Polished 2026-06-13.** Fixed empty state "Empty Creative Registry"/"backend"/"hydrate" → clean; header `themeLink('')` → `themeLink('/')`; footer logo `href="#"` → `themeLink('/')` (arrow→block body); copyright simplified to `CRTV`. Lint 0 errors, HTTP 200. |
| 46 | `services_health` | Services | P2 | **Polished 2026-06-13.** Heavy internal language cleanup: `VITALITY PROTOCOL`→`Precision Healthcare`, `INITIALIZE CONSULTATION`→`Book a Consultation`, `VIEW CLINICIANS`→`View Clinicians`, `High-fidelity sync`/`wellness nodes` → plain language, `OFFICIAL REGISTRY`→`Our Specialists`, `CLINICAL TIERS`→`Care Plans`, `INITIALIZE` plan btn→`Get Started`, `CLINICAL GRADE V2`→`Platform Verified`; footer `distribution node`/`synchronizing telemetry` → clean; header/footer logos → `<a href={themeLink('/')}>` (block body); copyright cleaned. Seeder updated. Lint 0 errors, HTTP 200. |
| 47 | `autos_luxury` | Autos | P2 | **Polished 2026-06-13.** Fixed hero secondary CTA `href="#contact"` → `themeLink('/explore')`; footer logo `<h4>` → `<a href={themeLink('/')}>` (added `themeLink` to block body); copyright capitalization fixed. Lint 0 errors, HTTP 200. |
| 48 | `autos_electric` | Autos | P2 | **Polished 2026-06-13.** Added `useAutosThemeLink` import to components; fixed header + footer logo `href="#"` → `themeLink('/')`. Lint 0 errors, HTTP 200. |
| 49 | `autos_classic` | Autos | P3 | **Polished 2026-06-13.** Added `useAutosThemeLink`; fixed header + footer logo `href="#"` → `themeLink('/')`; auction card "Place Bid Now" `href="#"` → `themeLink('/explore')` (arrow→block body). Lint 0 errors, HTTP 200. |
| 50 | `events_music` | Events | P3 | **Polished 2026-06-13.** Fixed `heroEyebrow` `'PULSE // LIVE TRANSMISSION'`→`'Featured Events'`; metrics bar cleaned; `supportEyebrow` `'Underground Nodes'`→`'Supporting Acts'`; hero/cta descriptions cleaned of "sonic network"/"high-fidelity"/"Initialize"/"distribution" language; empty state `'Empty Event Registry'`→`'No Events Yet'`. SonicHeader logo `<div>`→`<a href={themeLink('/')}>`. VoltageFooter: added `useEventsThemeLink`, logo linked, description and copyright `'PULSE EVENTS // SONIC_REGISTRY_V2'`→`'PULSE. All rights reserved.'`. PulseExperience: description cleaned, callout `'NEXT_UP: IBIZA_MESH'`→`'Next Up: Ibiza'`, stat labels de-screamed. ThemeSeeder updated. Lint 0 errors, HTTP 200. |
| 51 | `events_festival` | Events | P3 | **Polished 2026-06-13.** Cleaned `heroEyebrow` `'THE_GLOBAL_COLLECTIVE_V8'`→`'Global Festival Lineup'`; hero description, secondary CTA `'Join_The_Pulse'`→`'Join the Pulse'`; `registryEyebrow` `'OFFICIAL_FESTIVAL_REGISTRY'`→`'Festival Lineup'`; registry description; `ctaEyebrow` `'READY_TO_LOSE_CONTROL'`→`'Ready to Attend'`; CTA description; empty state. HUD labels de-protocolled. Date format `JAN_05_2026`→`JAN 05 2026`; `'Global Node'`→`'Global'`. FestivalHeader logo `<div>`→`<a href={themeLink('/')}>`, button text `_ACTIVE` suffix removed. StageLineupCard `'SECURE_PASS ->'`→`'Get Tickets →'`. NexusFooter: added `useEventsThemeLink`, logo linked, description and copyright `'SELLIO_NEON_NODE // VIBE_STABLE'`→clean. ThemeSeeder updated. Lint 0 errors, HTTP 200. |
| 52 | `events_creative` | Events | P3 | **Polished 2026-06-13.** Cleaned `heroEyebrow` `'SYNTHETIC_CULTURE_EXCHANGE // 2026'`→`'Creative Events 2026'`; hero/registry/lab/sync descriptions and labels cleaned of "decentralized"/"node"/"synthetic"/"bio-digital"/"algorithmic" language; HUD labels de-protocolled; empty state fixed; capabilities `'Synthetizers|Generators|Decentralizers|Transmitters'`→real event types. `formatEventDateUnderscore` shared utility fixed: `'DATE_TBA'`→`'Date TBA'`, underscores→spaces (also fixes events_classic date display). CreativeHeader logo `<div>`→`<a href={themeLink('/')}>`, button `: ACTIVE` suffix removed. ArtisanEventCard `'SYNC_NODE ->'`→`'Get Tickets →'`. VibrantFooter: added `useEventsThemeLink`, logo linked, description and copyright `'SELLIO_CREATIVE_NODE // PULSE_STABLE'`→clean. ThemeSeeder updated. Lint 0 errors, HTTP 200. |

## Recommended Workflow

Finalize in batches:

1. **P0 themes:** ranks 1-10. These should be polished before any CodeCanyon submission screenshots are captured.
2. **P1 themes:** ranks 11-23. These should be reliable enough for reviewer/demo exploration.
3. **P2 themes:** ranks 24-48. These should avoid broken UI, broken routing, and obvious static-data leaks.
4. **P3 themes:** ranks 49-52. These can be lighter polish as long as they do not break the demo.

For each theme, verify:

- homepage layout and responsive behavior
- listing/explore page
- detail/product page
- live API data or clear demo fallback behavior
- theme-aware links under `/preview/{theme_key}`
- empty states
- no overlapping text/buttons
- no broken images
- no console/runtime errors

## Progress Notes

### `unifieds_marketplace` - Polished 2026-06-12

Reference concept checked:

`D:\Sellio\_development\reference-library\REFERENCE_LIBRARY\unified\marketplace`

Result:

- The previous implementation partially carried the structure, but the concept had drifted into a dark TradeNode/exchange style.
- The homepage has been realigned to the reference marketplace hub concept with a blue/orange palette, search integration, featured listing carousel, category blocks, trending item cards, top-rated seller trust section, and clear marketplace CTAs.
- Existing Sellio behavior was preserved: API-backed products, theme-aware preview links, shared unified cart/checkout flow, and menu-driven header/footer.
- Verification passed with `npm.cmd run lint` and `npm.cmd run build` in `apps/storefront`.
- Second-pass cleanup fixed marketplace action buttons so header CTA menu items navigate correctly, removed one leftover sync-style sentence, and reverified `/preview/unifieds_marketplace` with HTTP 200.
- Preview stability fix: `getActiveTheme()` is now cached per request and preserves the requested preview theme as the offline fallback, preventing the page from rewrapping in `unifieds_default` if the active-theme API fails during preview.
- Live preview content fix: stale backend/theme default records for `unifieds_marketplace` were still serving the old Liquid Exchange hero/menu/footer copy. The frontend defaults, backend seeders, and current local DB records were updated to the MarketHub marketplace concept; `/preview/unifieds_marketplace` now returns no old Trade/Exchange hero or menu text.
- Premium delivery pass: shortened hero/API-backed copy, added hero proof chips, expanded trust metrics, applied stronger card/surface styling, added line clamps for live listing content, and reverified `/preview/unifieds_marketplace` with the premium MarketHub copy and wrapper intact.
- Competitive polish pass: upgraded the first-screen composition with marketplace snapshot stats, richer featured-card treatment, visible category inventory counts, stronger listing hover states, tighter mobile responsive guards, and a production build plus preview smoke check.
- Explore page refactor: replaced the generic shared unified explore page with a marketplace-specific directory experience including a premium hero, live inventory summary, vertical selector lanes, category chips, refined filter command bar, polished result cards, load-more support, and stronger empty/loading/error states. Verified `/preview/unifieds_marketplace/explore` and a vertical-filtered preview route with HTTP 200. Cross-vertical detail routing remains a follow-up because the global listing route currently resolves to the active theme `ProductPage`.
- Explore real-data fix: removed curated fallback/dummy explore records and switched the marketplace directory to real API listings from products, properties, vehicles, jobs, services, events, and classifieds. Verified all seven local `/api/v1/*` listing endpoints return HTTP 200 with records.
- Explore runtime fix: replaced direct `@sellio/api-client` catalog-method calls with direct public `/api/v1/{vertical}` fetches inside the marketplace explore page, preventing the Turbopack browser runtime from crashing when `api.getProductsCatalog` is unavailable on the client instance.
- Explore category-shape fix: normalized category/title fields from mixed vertical API responses before rendering, preventing React from receiving objects such as `{ id, title }` as card or filter text.

### `ecommerce_default` - Polished 2026-06-12

Reference concept checked:

`D:\Sellio\_development\reference-library\REFERENCE_LIBRARY\ecommerce\default`

Result:

- The reference ecommerce default concept centers on familiar shop behavior: category/search browsing, square product cards, price overlays, featured/stock signals, product detail, cart, checkout, and seller/product metadata.
- The current Next storefront already had the core page structure, so the polish focused on buyer-facing credibility rather than a broad redesign.
- Homepage copy was changed from internal sync/protocol wording to clear retail language.
- Product cards were upgraded to match the reference intent with square media, price overlay, featured badge support, category/SKU metadata, stock state, and a clear product action.
- The explore directory now reuses the same ecommerce card pattern and cleaner buyer copy.
- Product detail and footer copy were cleaned to remove old protocol language and mojibake characters.
- CSS now uses named section classes for the category strip, collection section, newsletter block, and footer instead of brittle inline-style responsive selectors.
- Verification passed with `npm.cmd run lint`, `npm.cmd run build`, `/preview/ecommerce_default` HTTP 200, and `/api/themes/active` with `X-Theme-Key=ecommerce_default` HTTP 200.
- Second-pass cleanup removed the last defensive `href="#"` fallback from category ribbons and reverified `/preview/ecommerce_default` with HTTP 200.
- Preview stability fix: `getActiveTheme()` is now cached per request and preserves the requested preview theme as the offline fallback, preventing the hero from losing its theme wrapper/CSS after a refresh or transient API failure.

### `ecommerce_fashion` - Polished 2026-06-13

Result:

- Removed all protocol/internal language from visible UI: hero eyebrow changed from `FALL_WINTER_2026_COLLECTION` to `Fall / Winter 2026`; collection eyebrow from `THE_AUTUMN_CAPSULE_V8` to `The Autumn Capsule`; philosophy eyebrow from `ATELIER_PHILOSOPHY_SYNC` to `ATELIER PHILOSOPHY`; side image labels from underscore-coded to readable (`Accessories 01`, `Ready to Wear 04`); season label from `AUTUMN_WINTER_26` to `AUTUMN / WINTER 26`.
- Replaced the TrendHUD tech-status metrics (`ATELIER_NODES: 08`, `SILHOUETTE_PRECISION: 100%`, `GLOBAL_SYNC: STABLE/OFFLINE`) with real fashion store metrics: `PIECES` (live product count), `DESIGN HOUSES: 6`, `FREE RETURNS: ALWAYS`, `WORLDWIDE SHIPPING: EXPRESS`. Eliminated the API-status leak where `GLOBAL_SYNC` showed `OFFLINE` to buyers.
- Fixed look number format from `LOOK_01` (underscore) to `LOOK 01` (space); fixed padStart so 15 products shows `15` not `015`.
- Fixed broken mobile responsive CSS: replaced fragile `section[style*="..."]` attribute selectors (which never matched React-rendered HTML) with proper named CSS classes.
- Extracted hero positioning, side-panel layout, metrics strip, lookbook header, and philosophy section from inline styles into named CSS classes (`ef-hero-caption`, `ef-hero-side-panel`, `ef-hero-side-caption`, `ef-hero-cta`, `ef-metrics-grid`, `ef-lookbook-header`, `ef-philosophy-section`, `ef-philosophy-inner`, `ef-philosophy-divider`).
- Fixed footer copyright from `© 2026 SELLIO_ATELIER_NODE // SILHOUETTE_STABLE` to `© 2026 Sellio Atelier. All rights reserved.`
- Fixed offline-state diagnostics copy to be buyer-friendly: `Collection Unavailable` / `Products could not be loaded. Showing a curated lookbook as a preview.`
- Fixed empty state text from `EMPTY_LOOKBOOK` to `EMPTY COLLECTION` with buyer-readable description.
- Updated ThemeSeeder to match all corrected content values so fresh seeds produce clean copy.
- Fixed shared `EcommerceExplorePage`: removed `PRODUCT_DIRECTORY` label and replaced "synchronized from your Sellio catalog" with buyer-facing "Browse and shop our complete product catalog."
- Verified: `npm.cmd run lint` (0 errors), `npm.cmd run build` (clean), `/preview/ecommerce_fashion` HTTP 200.

### `ecommerce_electronics` - Polished 2026-06-13

Result:

- Removed dead `diagnostics.*` content reads and the corresponding inline CSS block (`.el-diagnostics-card`, `.el-diagnostics-header`, `.el-diagnostics-trace`) from `Page.tsx`. The electronics theme already uses the shared `CatalogSyncAlert` component — the old local diagnostic panel was stale dead code.
- Switched `heroImage` and `promoImage` from `useThemeContent` (text hook) to `useThemeMedia` (image hook) so admin-uploaded images are served correctly.
- Added named CSS classes for hero image wrapper (`el-hero-image`), description (`el-hero-description`), CTA row (`el-hero-cta-row`), promo section (`el-promo-section`, `el-promo-content`, `el-promo-title`, `el-promo-description`, `el-promo-bg`) and extracted inline styles into those classes.
- Fixed broken mobile responsive CSS: replaced `section[style*="background: 'linear-gradient..."]` and `.el-hero > div:nth-child(3)` attribute/position selectors (which never matched React-rendered HTML) with proper named class selectors.
- Removed redundant `getThemeLink` alias function; kept the single `useEcommerceThemeLink()` usage pattern.
- Changed promo CTA from a `<button>` (no-op) to `<a href={themeLink('/explore')}>` so it routes correctly in both preview and live modes.
- Fixed promo description: removed mention of non-existent "interactive 3D configurator" → "Browse our full catalog of components and find everything you need to build or upgrade your rig."
- Fixed `ElectronicsHeader`: `href="#"` logo → `href={themeLink('/')}`, removed hardcoded cart badge count of 3, converted cart icon from `<div>` to `<a href={href}>` wired to the menu action system.
- Fixed `ElectronicsFooter`: `href="#"` logo → `href={themeLink('/')}`.
- Cleaned ThemeSeeder: removed unused `diagnostics.title` and `diagnostics.description` entries for `ecommerce_electronics`; updated `promo.description` and `promo.cta_label` to match current Page.tsx defaults. Also fixed stale `ecommerce_default` footer copyright from `SELLIO_SHOP // TRANSACTION_SYNC_STABLE` to `© 2026 Sellio Shop. All rights reserved.`
- Verified: `npm.cmd run lint` (0 errors), `npm.cmd run build` (clean), `/preview/ecommerce_electronics` HTTP 200.

### `properties_modern` - Polished 2026-06-13

Result:

- Fixed `CatalogRegistryAlert`: added `if (variant === 'demo') return null;` so demo/preview mode shows silently without a diagnostic banner. Removed `<pre>Error details: {error}</pre>`. Production variant now shows clean buyer-friendly copy.
- Fixed `UrbanHeader`: logo `<div className="urban-logo">` → `<a href={themeLink('/')}>` using `useModernThemeLink`, so the brand name links back to the homepage in both preview and live modes.
- ThemeSeeder data already clean (lives in `data/properties_modern_home_content.php` — no internal protocol language found).
- No broken `section[style*="..."]` CSS selectors found in `styles.css`.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_modern` HTTP 200.

### `properties_luxury` - Polished 2026-06-13

Result:

- Fixed all internal/protocol language throughout `Page.tsx`: `ESTABLISHED_REPRESENTATION` → `Exclusively Represented`; `EXPLORE_COLLECTION` → `Explore Collection`; `logic.items` bar changed from `ASSETS_UNDER_MANAGEMENT: $18.4B|NODAL_VERIFICATION: ELITE|...` to real buyer-facing metrics; `OFF_MARKET_NODES` → `Off-Market Listings`; `EDITORIAL_INSIGHT` → `Editorial`; `READ_THE_JOURNAL` → `Read the Journal`; `CONNECT_WITH_CONCIERGE` → `Connect with Concierge`. Removed "high-fidelity acquisition" and "distribution network" jargon from CTA copy.
- Converted all `<button>` CTAs in `Page.tsx` to `<a href={themeLink(...)}>` links so they actually navigate.
- Fixed `PlatinumHeader`: logo `<div className="platinum-logo">PLATINUM_ESTATE</div>` → `<a href={themeLink('/')}>PLATINUM.</a>`.
- Fixed `EstateShowcase`: replaced `CURATED_COLLECTION` kicker with `Curated Collection`; replaced internal `NEW_LISTING`/`FEATURED` tags with `New Listing`/`Featured`/`Signature`; removed full "System Connection Diagnostic Warning" + raw error `<pre>` panel shown to buyers in fallback mode — silently shows demo listings instead; replaced `EXPLORE_FULL_PORTFOLIO` button (dead) with `<a>View Full Portfolio</a>` link.
- Fixed `LuxuryAmenities`: `WHITE_GLOVE_EXPERIENCE` → `White Glove Experience`; removed "nodal representation" and "transportation nodes" — replaced with natural luxury real estate language.
- Fixed `ConciergeFooter`: copyright `© 2026 PLATINUM_ESTATE_GROUP. ALL_ASSETS_VERIFIED.` → `© 2026 Platinum Estate Group. All rights reserved.`; removed `v.1.0_LUXURY_ELITE` version string.
- Fixed `ExplorePage`: `PORTFOLIO_DIRECTORY` → `Portfolio Directory`; removed "Surgically filter and acquire premier assets from our high-fidelity, verified database network" copy; removed full diagnostic banner shown to buyers; filter placeholders `SEARCH_ASSETS...`/`ALL_CATEGORIES`/`ALL_LOCATIONS`/`MIN_BEDROOMS`/`PRICE_RANGE` → natural language; empty state copy; load-more button `LOAD_MORE_ASSETS`/`LOADING_ASSETS...` → `Load More`/`Loading...`. Dropped unused `useFallback` and `apiError` state variables.
- Updated ThemeSeeder: all `properties_luxury` content values now match the cleaned UI defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_luxury` HTTP 200.

### `properties_rental` - Polished 2026-06-13

Result:

- Fixed `CatalogRegistryAlert` (rental explore variant): `variant="demo"` now returns null; removed `<pre>Error details: {error}</pre>`; production copy simplified to buyer-friendly "check API connection" message.
- Fixed `RentalHeader`: logo `<div className="pr-logo">` → `<a href={themeLink('/')}>` — brand name now links home.
- Fixed `TenantFooter`: logo `<div className="pr-logo--footer">` → `<a href={themeLink('/')}>`.
- ThemeSeeder `properties_rental` section had 35 internal-language values: `EASY_LEASING_PROTOCOL_V8`, "high-fidelity rental protocol", "cryptographic lease signing nodes", `⚡ Synchronize Rental Search`, `DIGITAL_LEASES`, `MAINTENANCE_SLA`, `APPROVAL_SYNC`, `NODE_STATUS`, `RESET_FILTERS ⌄`, "Siloed verified residential units", `No Rental Nodes Found`, `AUTHORIZE_NEW_RESIDENCE`, `EXPLORE AVAILABLE UNITS`, and 4 `diagnostics.*` rows — all replaced with clean buyer-facing copy matching the Page.tsx defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_rental` HTTP 200.

### `properties_map` - Polished 2026-06-13

Result:

- Fixed `components/index.tsx` `MapHeader`: logo `<div>MAPNEXUS</div>` → `<a href={themeLink('/')}>MAPNEXUS</a>` using `usePropertyThemeLink`.
- Fixed `components/index.tsx` `MapHUD`: HUD overlay labels `SPATIAL_COORDINATES` → `Location` and `DISTRICT_INTEGRITY` → `Coverage` — these were visible on the map canvas as a dark glass card.
- Fixed `Page.tsx` sidebar defaults: `UNITS` → `listings`; `FILTER|PRICE|TYPE` → `Filter|Price|Type`; `END_OF_REGISTRY` → `End of results`; error state raw `{propertyError}` → static buyer-friendly message.
- Fixed ThemeSeeder `properties_map`: all 9 values updated to match cleaned defaults (`Registry Nodes` → `Properties`, `UNITS` → `listings`, `Empty Property Registry` → `No listings yet`, `END_OF_REGISTRY` → `End of results`, offline kicker/title cleaned up).
- Orphaned component files (`SpatialSyncBar.tsx`, `CartographicGrid.tsx`, `MapHeader.tsx`) identified as not exported from `components/index.tsx` — not used in any rendered path; left in place.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_map` HTTP 200.

### `unifieds_default` - Polished 2026-06-13

Result:

- Fixed all internal/protocol language in `Page.tsx` defaults: `FOUNDATIONAL_DISTRIBUTION_V1` → `Multi-Vertical Marketplace`; "high-fidelity foundational node for multi-vertical commerce" → plain description; `GET STARTED CORE` → `Explore listings`; `READ THE SPEC` → `Browse catalog`; `VERTICALLY_READY` → `Live listings`; `LIVE_REGISTRY` → `Live Catalog`; `Core Listings Feed.` → `Featured Listings.`; "enterprise-grade discovery" → plain copy; `EMPTY_REGISTRY` → `No listings yet`; "this feed will hydrate automatically" → plain admin instruction; `INITIALIZE CORE NODE` → `Browse the catalog`; CTA title `Scale with the Foundation.` → `Start browsing today.`; CTA description removed "Initialize your core node" / "high-fidelity distribution network".
- Fixed hardcoded stats labels: `LIVE LISTINGS` → `Live listings`; `ACTIVE CATEGORIES` → `Active categories`; `IN STOCK NOW` → `In stock now`.
- Fixed product fallback description: "Verified marketplace listing synchronized from the Sellio catalog." → "Browse this listing for full details and pricing."
- Fixed `CoreFeatures` component: removed CRITICAL `onClick={() => alert(...)}` from all three feature cards; replaced dev-speak features (`Siloed Node Protocol`, `High-Fidelity Engine`, `Unified Analytics HUD`) with real buyer-facing features (`Multi-Vertical Commerce`, `Fast & Responsive`, `Seller Analytics`); fixed `CORE_COMPETENCIES` kicker → `Why choose us`.
- Fixed `GlobalTrust` bar: replaced `100%_SECURE_SYNC`, `ENTERPRISE_READY`, `SUB_MILLISECOND_LATENCY`, `CONNECTED_INTELLIGENCE` with `Secure Payments`, `Verified Sellers`, `Fast Delivery`, `Multi-Vertical Platform`.
- Fixed `InstitutionalFooter`: logo `<div className="ud-logo">COREORIGIN</div>` → `<a href={themeLink('/')}>` using `useUnifiedThemeLink`; footer description "high-fidelity core distribution platform for global commerce pipelines" → plain description; copyright `© 2026 SELLIO_CORE_OS // HORIZON_SYNC_STABLE` → `© 2026 Sellio. All rights reserved.`
- Updated ThemeSeeder `unifieds_default` section: all 15 content values cleaned to match the updated code defaults (eyebrow, description, CTA labels, badge label, all three stats labels, collection eyebrow/title/description, offline kicker/title, empty kicker/description, CTA title/description/button).
- Verified: `npm.cmd run lint` (0 errors), `/preview/unifieds_default` HTTP 200.

### `unifieds_modern` - Polished 2026-06-13

Result:

- Fixed all internal/protocol language in `Page.tsx` defaults: `CORE_V4_PROTOCOL` → `Modern Marketplace`; "high-fidelity distribution node for multi-vertical commerce" → plain buyer description; `INITIALIZE NODE` → `Browse the catalog`; `VIEW ARCHITECTURE` → `Explore listings`; `1.4M_NODES_ACTIVE` → `1.4M+ Active listings`; `LATENCY: 8ms` → `Free shipping available`; `DISTRIBUTION_READY: TRUE` → `Verified sellers`; `ENCRYPTION: AES_256` → `Secure checkout`; `LIVE_NEXUS_FEED` → `Live Catalog`; `Synchronized Listings.` → `Featured Listings.`; "Nexus Prime catalog layer for high-fidelity marketplace discovery" → plain; `NEXUS_OFFLINE` → `Connection error`; `EMPTY_NEXUS_FEED` → `No listings yet`; "nexus feed will hydrate automatically" → plain; mid-section title `The Power of Fifty.` → `The Power of One.`; mid-section description removed Nexus Prime jargon; CTA title `Ready to synchronize?` → `Ready to get started?`; CTA description removed "Initialize your high-fidelity storefront node"; `CONNECT CORE NODE` → `Browse the catalog`.
- Fixed showcase bullet list: `Dynamic Schema Mapping`, `Real-time Global Sync`, `High-Fidelity UI DNA`, `Institutional Security Nodes` → `Multi-Vertical Platform`, `Real-Time Catalog Updates`, `Mobile-First Design`, `Secure Transactions`.
- Fixed product card: `NEXUS_{product.id}` badge → `#{product.id}`; fallback description "synchronized into the Nexus Prime catalog" → "Browse this listing for full details and pricing."; "Open Sync" CTA → `View listing`; `'Sync quote'` price fallback → `'Price on request'`.
- Converted hero CTA buttons from `<button onClick={() => router.push(...)}>` to `<a href={themeLink(...)}>` links; same for the final section CTA. Removed the now-unused `useRouter` import.
- Fixed `NexusHeader`: logo `<div className="unp-logo">NEXUSPRIME</div>` → `<a href={themeLink('/')}>` using `useUnifiedThemeLink`.
- Fixed `NexusBentoGrid`: `CORE_ENGINE` kicker → `One Platform`; bento side copy "Real-time transactions per node." → "Products available across all verticals."; "High-Fidelity Sync Active." → "Catalog updated live."
- Fixed `NexusPricing`: `NODAL_SUBSCRIPTION` kicker → `Subscription Plans`; `Siloed Capacity Plans.` → `Plans & Pricing.`; "distribution subscriptions" → plain; plan features replaced ("Core Schema Mapping", "1ms Custom Pipelines", "Sovereign Sub-Second Sync", "Nodal Operations") with buyer-facing copy; plan button `ACTIVATE NODE` → `<a>Get started</a>` linking to `/explore`.
- Fixed `NexusFooter`: logo `<div>NEXUSPRIME</div>` → `<a href={themeLink('/')}>` link; description "advanced heavyweight app-like distribution node" → plain; copyright `© 2026 SELLIO_NEXUSPRIME_OS // CORE_ACTIVE` → `© 2026 Sellio. All rights reserved.`
- Updated ThemeSeeder `unifieds_modern` section: all 24 content values cleaned to match updated code defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/unifieds_modern` HTTP 200.

### `classifieds_modern` - Polished 2026-06-13

Result:

- Fixed `ModernHeader`: logo `<a href="#" onClick={reload}>` → `<a href={themeLink('/')}>` using `useClassifiedsThemeLink`. Converted from arrow-function expression to block body to allow hook usage.
- Fixed `ModernFooter`: logo `<a href="#" onClick={preventDefault}>` (completely dead) → `<a href={themeLink('/')}>`. Copyright removed "Envato Elite Standard" → `© 2026 Sellio. All rights reserved.`
- Fixed `Page.tsx`: location fallback `'Classified Network'` → `'Location not specified'`.
- Removed two dead `diagnostics.title`/`diagnostics.trace` rows from ThemeSeeder `classifieds_modern` section — not read by any component.
- `CatalogSyncAlert` (classifieds shared) already had `variant='demo' → return null` and no raw `{error}` rendering — no changes needed.
- Verified: `npm.cmd run lint` (0 errors), `/preview/classifieds_modern` HTTP 200.

### `classifieds_general` - Polished 2026-06-13

Result:

- Fixed `GeneralHeader`: logo `<a href="#" onClick={reset}>` → `<a href={themeLink('/')}>` using `useClassifiedsThemeLink` (added import).
- Fixed `GeneralFooter`: `footer.description` default "Engineered to Elite Standards." → `© 2026 Sellio. All rights reserved.`
- Fixed `Page.tsx`: `loadingMoreLabel` default `'Syncing Classifieds...'` → `'Loading more...'`
- Updated ThemeSeeder: `loading_more_label` and `footer.description` values to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/classifieds_general` HTTP 200.

### `classifieds_premium` - Polished 2026-06-13

Result:

- Fixed `PremiumFooter`: logo `<a href="#" onClick={preventDefault}>` → `<a href={themeLink('/')}>` using `useClassifiedsThemeLink`; description "high-net-worth investors with established business acquisitions" → plain marketplace description; copyright removed "Vetted Network"; removed `🔒 Escrow Secured Node • Elite Sovereign Standards` span entirely.
- Fixed `PremiumCard`: button `'View Memorandum & Details'` → `'View Details'` (M&A jargon removed).
- Fixed `Page.tsx`: `membershipSubtitle` default removed "institutional-grade M&A prospectuses, audit-vetted tax returns" → plain buyer-facing copy.
- Removed two dead `diagnostics.title`/`diagnostics.description` rows from ThemeSeeder; updated `membership.subtitle` to match cleaned default.
- Verified: `npm.cmd run lint` (0 errors), `/preview/classifieds_premium` HTTP 200.

### `jobs_corporate` - Polished 2026-06-13

Result:

- `CorporateHeader` and `CorporateFooter` logos already used `themeLink('')` — no fix needed.
- Fixed `Page.tsx`: `emptyKicker` default `'Empty Job Registry'` → `'No roles yet'`; `emptyDescription` removed "backend" and "hydrate automatically" language → plain admin instruction.
- Fixed `components/index.tsx`: `CorporateFooter` `copyright` default `'© 2026 TalentCorp Inc. All rights reserved.'` → `'© 2026 Sellio. All rights reserved.'`
- Removed two dead `sync.offline_kicker`/`sync.offline_title` rows from ThemeSeeder `jobs_corporate` section — not referenced by any component; updated `empty.kicker`, `empty.description`, and `footer.copyright` values to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/jobs_corporate` HTTP 200.

### `jobs_tech` - Polished 2026-06-13

Result:

- Fixed `TechHeader`: logo `<a href="#">` → `<a href={themeLink('/')}>` using `useJobsThemeLink` (added import).
- Fixed `TechFooter`: logo `<a href="#">` → `<a href={themeLink('/')}>` using `useJobsThemeLink`; copyright default `'© 2026 DevJobs. All rights reserved.'` → `'© 2026 Sellio. All rights reserved.'`
- Fixed `Page.tsx`: `emptyDescription` default `'Adjust your grep filters or tags to search alternative developer channels.'` → `'Try different filters or search terms to find relevant roles.'`
- Removed two dead `diagnostics.title`/`diagnostics.description` rows from ThemeSeeder `jobs_tech` section; updated `collection.refresh_label` `'./refresh_catalog.sh'` → `'Browse all roles'`; `empty.description` and `footer.copyright` updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/jobs_tech` HTTP 200.

### `services_marketplace` - Polished 2026-06-13

Result:

- `MarketplaceHeader` logo already uses `themeLink('')` — no fix needed.
- Fixed `MarketplaceFooter`: hardcoded copyright `© 2026 ServiceConnect. All rights reserved.` → `© 2026 Sellio. All rights reserved.`
- Fixed `Page.tsx`: empty categories state copy `"services are synced from the backend"` → `"services are added in the admin panel"`
- ThemeSeeder `services_marketplace` section: all values already clean — no changes needed.
- Verified: `npm.cmd run lint` (0 errors), `/preview/services_marketplace` HTTP 200.

### `services_corporate` - Polished 2026-06-13

Result:

- `CorporateHeader` logo already uses `themeLink('')` — no fix needed.
- Fixed `CorporateFooter`: hardcoded copyright `© 2026 Corporate Services. All rights reserved.` → `© 2026 Sellio. All rights reserved.`
- Fixed `Page.tsx` empty state: `"Empty Service Registry"` → `"No services yet"`; `"backend"` and `"hydrate automatically"` language → plain admin instruction.
- Fixed `Page.tsx` service card fallback description: `"A live service record synchronized from the Sellio service catalog."` → `"Browse this service for full details and pricing."`
- ThemeSeeder `services_corporate` section: all values already clean — no changes needed.
- Verified: `npm.cmd run lint` (0 errors), `/preview/services_corporate` HTTP 200.

### `autos_modern` - Polished 2026-06-13

Result:

- Header logo already uses `themeLink('/')` via `Link href` — no fix needed.
- Footer logo already uses `themeLink('/')` via `Link href` — no fix needed.
- Fixed `ModernFooter` and ThemeSeeder: `footer.copyright` default `'2026 Modern Autos, Inc. All rights reserved.'` → `'2026 Sellio. All rights reserved.'`
- Page.tsx empty state, hero CTAs, and all content already clean — no further changes needed.
- Verified: `npm.cmd run lint` (0 errors), `/preview/autos_modern` HTTP 200.

### `autos_used` - Polished 2026-06-13

Result:

- Fixed `UsedHeader`: logo `<a href="#">` → `<a href={themeLink('/')}>` using `useAutosThemeLink` (added import).
- Fixed `UsedFooter` and ThemeSeeder: `footer.copyright` default `'2026 DriveHub Marketplace. All rights reserved.'` → `'2026 Sellio. All rights reserved.'`
- Page.tsx content already clean — no further changes needed.
- Verified: `npm.cmd run lint` (0 errors), `/preview/autos_used` HTTP 200.

### `events_classic` - Polished 2026-06-13

Result:

- Fixed `VenueHeader`: logo `<div className="ecl-logo">` → `<a href={themeLink('/')}>` using `useEventsThemeLink` (added import).
- Fixed action button render: removed ` ACTIVE` suffix appended to all action button labels (appeared twice — mobile and desktop).
- Fixed `OccasionCard`: `'REQUEST PATRON PASS ->'` → `'VIEW EVENT →'`
- Fixed `LegacyFooter`: description default "Synchronizing institutional archives with global patron nodes" → plain copy; copyright `'(c) 2026 SELLIO_LEGACY_ARTS // ARCHIVE_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Fixed `Page.tsx`: `trust.items` `AUTHENTIC_INSTITUTIONAL_NODES|...` → buyer-facing labels; `collection.eyebrow` `'OFFICIAL_CULTURAL_REGISTRY'` → `'Event Catalog'`; `collection.description` removed "unified protocol synchronizes" language; `patron.eyebrow` `'PATRON_CIRCLE_PROTOCOL'` → `'Membership Circle'`; `patron.description` removed "Sellio Legacy protocol"; `patron.perks` underscore items → space-separated; `patron.card_description` removed "Institutional inquiry nodes"; `patron.card_cta_label` `'Request Institutional Access'` → `'Request Membership Access'`.
- Fixed hardcoded `BookingHUD` labels: `"VERIFIED_VENUES"` → `"Verified Venues"`, `"INSTITUTIONAL_NODES"` → `"Partner Venues"`, `"PATRON_SYNC_SPEED"` → `"Response Time"`, `"ARCHIVE_STABILITY"` → `"Uptime"`.
- Fixed empty state: `'Empty Event Registry'` → `'No events yet'`; "backend" and "hydrate automatically" → plain admin instruction.
- Updated ThemeSeeder `events_classic` section: all 9 affected values cleaned.
- Verified: `npm.cmd run lint` (0 errors), `/preview/events_classic` HTTP 200.

### `events_corporate` - Polished 2026-06-13

Result:

- `Header` logo already uses `<Link href={themeLink('')}>` — no fix needed.
- Fixed `Footer`: copyright `'© 2026 SELLIO_EVENTS_GRP'` → `'© 2026 Sellio. All rights reserved.'`
- Fixed `Page.tsx` eyebrow labels: `'WORLD_ENGINEERING SUMMIT // 2026'` → `'WORLD ENGINEERING SUMMIT // 2026'`; `'CONVENTIONS_CATALOG // DIRECTORY'` → `'EVENT CATALOG // DIRECTORY'`; `'FACULTY_SYNC // 2026'` → `'FEATURED SPEAKERS // 2026'`; `'CURATED_SCHEDULE // DAY_01'` → `'CURATED SCHEDULE // DAY 01'`.
- Fixed `agendaCta` default: `'DOWNLOAD FULL PROGRAM PDF'` (was a dead/misleading label — the button links to `/explore`, not a PDF) → `'BROWSE FULL CATALOG'`.
- Updated ThemeSeeder `events_corporate` section: 5 eyebrow/label values and copyright updated.
- Verified: `npm.cmd run lint` (0 errors), `/preview/events_corporate` HTTP 200.

### `properties_classic` - Polished 2026-06-13

Result:

- Fixed `Page.tsx`: `heroDescription` default removed "curated distribution" → "curated collection … Every estate is verified for architectural provenance and heritage integrity."; `collectionDescription` removed "distribution includes verified manorial rights" → "The collection includes historic estates with verified architectural provenance and heritage significance."
- Fixed hardcoded eyebrow in listing section: `'Collection Node // 01'` → `'The Collection // 01'`; fallback label `'Demo Catalogue // Preview'` → `'Sample Properties // Preview'`.
- Fixed empty state title: `'No Listings Registered'` → `'No Properties Found'`; description made more natural.
- Fixed load-more button labels: `'VERIFYING PROVENANCE...'` → `'Loading...'`; `'LOAD MORE PROVENANCE'` → `'LOAD MORE ESTATES'`.
- Fixed `LegacyFooter.tsx`: `footer.eyebrow` default `'Global Registry // Footer'` → `'Heritage Collection'`; `footer.description` removed "curated distribution" → "curated collection … verified for architectural provenance and heritage value."; `footer.subscribe_text` removed "distribution protocol" → plain newsletter copy; `footer.copyright` `'© 2026 ESTATE & HERITAGE // GLOBAL REGISTRY'` → `'© 2026 Sellio. All rights reserved.'`
- Fixed `CatalogRegistryAlert.tsx`: added `if (variant === 'demo') return null;` to match other vertical patterns; removed `isDemo` conditional branching — component now only renders the production error state; removed "System Diagnostic:" internal label; simplified production copy to buyer-friendly language.
- `HeritageHeader.tsx` logo already uses `getThemeLink('/', themeKey, isPreview)` — no fix needed.
- Updated ThemeSeeder `properties_classic` section: `hero.description` and `collection.description` updated; added new `footer.eyebrow` and `footer.copyright` rows (previously missing); updated `footer.description` and `footer.subscribe_text`.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_classic` HTTP 200.

### `properties_vacation` - Polished 2026-06-13

Result:

- Fixed `Page.tsx` content defaults: `hero.kicker` `'GLOBAL_ESCAPE_REGISTRY_V8'` → `'Global Vacation Retreats'`; `hero.description` "Authenticated by our local nodes" → "Verified by local experts and enjoyed by travelers worldwide."; `grid.kicker` `'CURATED_COLLECTION'` → `'Curated Collection'`; `grid.description` "local node expert" → handpicked/verified language; `philosophy.kicker` `'THE_GETAWAY_PROTOCOL'` → `'The Getaway Philosophy'`; `philosophy.description` "every vacation node" → "every retreat"; `trust.items` all four underscore-coded items → `'100% Verified|No Hidden Fees|Local Expert Support|Secure Booking'`.
- Fixed hardcoded stat labels in JSX: `"LIVE RETREATS"` → `"Live Retreats"`; `"UNIQUE LOCATIONS"` → `"Unique Locations"`.
- Fixed `VacationHeader`: added `usePropertyThemeLink` import; logo `<div className="pv-logo">` → `<a href={themeLink('/')}>` linking homepage.
- Fixed `RetreatBentoCard`: `'AUTHENTICATED RETREAT'` → `'Verified Retreat'`; `'SECURE BOOKING →'` → `'Book Now →'`.
- Fixed `EscapeFooter`: converted from arrow expression to block body to allow hook usage; logo `<div>ESCAPENODE</div>` → `<a href={themeLink('/')}>` link; description removed "high-fidelity vacation registry" and "Synchronizing" → "Discover the world's finest vacation retreats, verified by local experts…"; copyright `'© 2026 SELLIO_VACATION_OS // HORIZON_SYNC_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Cleaned ThemeSeeder `properties_vacation` section: removed 20 dead rows (`search.*` ×10, `diagnostics.*` ×2, `ribbon.all_label`, `empty.*` ×3, `philosophy.stat_*` ×4 — none read by any component); updated 8 live rows; fixed `philosophy.badge_label` `'AUTHENTICATED LOCAL RETREAT'` → `'Verified Local Retreat'`.
- `CatalogSyncAlert.tsx` (vacation-local variant) already shows only production errors — no fix needed.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_vacation` HTTP 200.

### `properties_commercial` - Polished 2026-06-13

Result:

- Fixed `Page.tsx` inline content defaults (heavy internal language throughout): `hero.kicker` `'COMMERCIAL_REGISTRY_V8_DISTRIBUTION'` → `'Commercial Real Estate'`; `hero.description` removed "authoritative commercial registry" and "institutional-grade"; `hero.stat_label` `'QUARTERLY_TURNOVER'` → `'Quarterly Turnover'`; `hero.primary_cta_label` `'Explore_Inventory'` → `'Explore Listings'`; `hero.secondary_cta_label` `'Request_Appraisal'` → `'Request Appraisal'`; `intelligence.description` removed "high-fidelity" and "registry" language; HUD labels `'DUE_DILIGENCE_SPEED'`/`'AVG_YIELD_v2026'`/`'GLOBAL_NODES'` → human-readable; `inventory.kicker` `'INSTITUTIONAL_INVENTORY'` → `'Commercial Inventory'`; `inventory.title` `'Asset Registry.'` → `'Asset Directory.'`; `inventory.description` removed "unified protocol" / "authoritative node"; filter labels `'SEARCH_QUERY'`/`'ASSET_CLASSIFICATION'`/`'ACQUISITION_STATUS'` → readable; filter option text `'ALL_CLASSIFICATIONS'`, `'PRIME_OFFICE'`, etc. → readable; status options `'ALL_STATUSES'`, `'PRIVATE_SALE'` → readable; empty state `'REGISTRY_RESOLVE_NULL'`/`'No Assets Resolved'` → readable; trust bar `'AS_FEATURED_IN:'` → `'As Featured In'`; brand names `'FINANCIAL_TIMES'` etc. → readable; CTA kicker `'INSTITUTIONAL_ACQUISITION'` → `'Get Started'`; CTA button `'Request_Institutional_Access'` → `'Request Access'`.
- Fixed inline diagnostics panel: removed `useThemeContent('diagnostics.*')` reads that injected raw error code + internal kicker into buyer-facing UI; replaced with a clean static demo notice.
- Fixed fallback asset descriptions (6 entries): removed "server vaults", "fiber optical routing hubs", "transport routing protocols", "subterranean EV storage clusters" internal jargon; generic property description fallback removed "high-fidelity institutional-grade".
- Fixed `CommercialHeader`: added `usePropertyThemeLink` import; logo `<div className="pc-logo">` → `<a href={themeLink('/')}>` linking homepage.
- Fixed `AssetRegistryCard`: `'TOTAL_AREA'` → `'Total Area'`; `'STATUS'` → `'Status'`; `'REQUEST_AUDIT'` → `'Request Info'`; `'VIEW_YIELD →'` → `'View Details →'`.
- Fixed `InstitutionalFooter`: converted from arrow expression to block body; added `usePropertyThemeLink`; logo `<div>CORP</div>` → `<a href={themeLink('/')}>` link; description removed "global authoritative registry" and "Synchronizing yield and structural metadata"; copyright `'© 2026 SELLIO_COMMERCIAL_GRP // NODE_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Cleaned ThemeSeeder `properties_commercial` section: removed 3 dead `diagnostics.*` rows (no longer read by any component); updated 20 live rows to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_commercial` HTTP 200.

### `properties_investment` - Polished 2026-06-13

Result:

- Fixed `Page.tsx` inline content defaults: `hero.kicker` `'PORTFOLIO_SYNC_V8_ACTIVE'` → `'Investment Portfolio'`; `hero.title`/`hero.highlight` changed "Synchronized" → "Verified"; `hero.description` removed "high-fidelity terminal", "asset nodes", "performance-driven precision"; hero buttons `'Execute_Investment'`/`'View_Reports'` → `'Explore Properties'`/`'View Reports'`; converted hero CTA buttons from dead `<button>` elements to `<a href={themeLink('/explore')}>` links; `terminal.metrics_label` `'NETWORK_PERFORMANCE_METRICS'` → `'Portfolio Metrics'`; all four HUD labels `'TOTAL_NETWORK_VOLUME'`/`'AVERAGE_YIELD_ARR'`/`'LIQUIDITY_INDEX'`/`'VOLATILITY_HEDGE'` → readable; `hud.volatility_value` `'ACTIVE'` → `'Low'`; `logic.items` pipe-separated items replaced (all 4 had underscore + colon format); `grid.kicker` `'YIELD_REGISTRY'` → `'Asset Performance'`; `grid.description` removed "unified protocol synchronizes" / "yield nodes"; offline state "registry could not be loaded" → "Properties could not be loaded."; raw `{propertyError}` replaced with static buyer-friendly message; empty state `'Empty Property Registry'`/`'backend'`/`'hydrate automatically'` → clean copy; CTA kicker `'INSTITUTIONAL_GRADE_LOGIC'` → `'Get Started'`; CTA description removed "investment nodes", "capital node", "high-fidelity asset distribution"; CTA button `'Connect_Capital_Node'` → `'Explore Opportunities'` with `<a>` link.
- Fixed `InvestmentHeader`: added `usePropertyThemeLink` import; logo `<div className="pi-logo">` → `<a href={themeLink('/')}>` link; `NETWORK_STABLE_V8` status badge (mobile + desktop) → `Sellio Platform`.
- Fixed `PortfolioAssetCard`: `'EXECUTE →'` → `'View →'`.
- Fixed `InstitutionalFooter`: converted from arrow expression to block body; logo `<div>YIELDNODE</div>` → `<a href={themeLink('/')}>` link; description removed "high-fidelity terminal" and "Synchronizing capital distribution with verified asset nodes"; copyright `'© 2026 SELLIO_CAPITAL_GRP // SYNC_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `properties_investment` section: all 27 values updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_investment` HTTP 200.

### `properties_showcase` - Polished 2026-06-13

Result:

- Fixed `Page.tsx` inline content defaults: `hero.kicker` `'CURATED_ATELIER_COLLECTION_V8'` → `'Curated Atelier Collection'`; `hero.description` removed "curated distribution" and "Synchronizing institutional curation with museum-grade provenance" → clean collection description; `hero.secondary_cta_label` `'READ_MANIFESTO'` → `'Read the Manifesto'`; converted secondary hero button from dead `<button onClick={router.push}>` to `<a href={themeLink('/')}>` link; removed now-unused `useRouter` import and `router` variable.
- Fixed curator section defaults: `curator.description` "Atelier registry" and "each node" → "Atelier collection" / "each estate"; all three `curator.stat_*_value` defaults from all-caps (`'INSTITUTIONAL'`/`'MUSEUM'`/`'GLOBAL'`) to title case; all three `curator.stat_*_label` defaults from underscore-coded (`'CURATION_TIER'`/`'GRADE_PROVENANCE'`/`'DISTRIBUTION_SYNC'`) to readable labels.
- Fixed property description fallback: `'A curated architectural node synchronized from the Sellio property registry.'` → `'A distinguished property with verified architectural and heritage significance.'`
- Fixed offline state: `offline.kicker` `'Property Sync Offline'` → `'Connection Error'`; raw `{propertyError}` replaced with static buyer message.
- Fixed empty state: `empty.kicker` `'Empty Property Registry'` → `'No Properties Yet'`; description removed "backend" and "hydrate automatically".
- Fixed philosophy bar: `'INSTITUTIONAL_CURATION|ARCHITECTURAL_INTEGRITY|HISTORIC_PRESERVATION|EDITORIAL_SYNC'` → `'Institutional Curation|Architectural Integrity|Historic Preservation|Editorial Excellence'`.
- Fixed CTA section: `cta.kicker` `'BEGIN_YOUR_CURATION'` → `'Begin Your Curation'`; `cta.title` `'Authorize Your \nCollection.'` → `'Begin Your \nCollection.'`; `cta.description` removed "institutional nodes" / "Submit your provenance for review" → plain inquiry copy; CTA button converted from dead `<button onClick={router.push('/')}>` to `<a href={themeLink('/explore')}>` link.
- Fixed `ArtisanHeader`: added `usePropertyThemeLink` import; logo `<div className="ps-logo">` → `<a href={themeLink('/')}>` linking homepage; `CURATION_SYNC_ACTIVE` status badges (mobile + desktop) → `Sellio Platform`.
- Fixed `CinematicPropertyCard`: `FEATURED_NODE` badge → `Featured`; `VIEW_PROVENANCE_DATA →` dead `<button>` → `View Details →` div (card is wrapped in `<a>` in Page.tsx — nested interactive elements removed).
- Fixed `EditorialFooter`: converted from arrow expression to block body to allow hook usage; added `usePropertyThemeLink`; logo `<div className="ps-logo">ATELIER</div>` → `<a href={themeLink('/')}>ATELIER</a>` link; description removed "high-fidelity distribution" and "Synchronizing institutional curation"; copyright `'© 2026 SELLIO_ATELIER_NODE // DATA_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `properties_showcase` section: renamed dead `story.*` rows (not read by any component) to `curator.*` (matching what the component actually reads); renamed dead `stats.*` rows to `curator.stat_*` keys; updated all 20 values to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_showcase` HTTP 200.

### `properties_platinum` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();` (router was declared but never called in the current component).
- Fixed `Page.tsx` inline content defaults: `hero.kicker` `'ARCHITECTURAL_SUBLIMITY_V8'` → `'Platinum Estates'`; `protocol.description` "every node in our network" → "every property in our portfolio"; `protocol.stat_1_label` `'OFF_MARKET_NODES'` → `'Off-Market'`; `protocol.stat_2_label` `'ASSETS_UNDER_SYNC'` → `'Assets Under Management'`; `protocol.stat_3_label` `'GLOBAL_CONCIERGE'` → `'Global Concierge'`; `showcase.kicker` `'CINEMATIC_SHOWCASE'` → `'Property Showcase'`; `showcase.filter_label` `'FILTER: LUXURY_TIER == "PLATINUM"'` → `'Platinum Collection'`; `showcase.nodes_suffix` `'NODES'` → `'Properties'`; `offline.kicker` `'Property Sync Offline'` → `'Connection Error'`; raw `{propertyError}` replaced with static buyer message; `empty.kicker` `'Empty Property Registry'` → `'No Properties Yet'`; `empty.description` removed "backend"/"hydrate automatically"; `cta.kicker` `'PRIVATE_CONSULTATION'` → `'Private Consultation'`; `cta.button_label` `'REQUEST_INVITATION'` → `'Request an Invitation'`.
- Fixed `Header`: added `usePropertyThemeLink` import; logo `<div className="pl-logo">` → `<a href={themeLink('/')}>` linking homepage.
- Fixed `ShowcaseCard`: `CERTIFIED_ACQUISITION` tag → `Verified`.
- Fixed `Footer`: converted from arrow expression to block body; added `usePropertyThemeLink`; logo `<div className="pl-logo pl-footer-logo">PLATINUM</div>` → `<a href={themeLink('/')}>PLATINUM</a>` link; description removed "distribution network" and "ultra-high-fidelity"; copyright `'© 2026 SELLIO_PLATINUM_GRP // NODE_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `properties_platinum` section: renamed dead `stats.*` rows to `protocol.stat_*` (matching component reads); renamed dead `showcase.filter_prefix`/`showcase.filter_suffix` to `showcase.filter_label`/`showcase.nodes_suffix`; updated all 23 values.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_platinum` HTTP 200.

### `properties_urban` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `Page.tsx`: `hero.kicker` `'URBAN_LIVING_V8_DISTRIBUTION'` → `'Urban Living'`; `hero.description` removed "high-fidelity city"; location fallback `'Urban Node'` → `'Urban Location'`; `intel.description` removed "urban nodes" and "high-fidelity smart-grid"; all four intel stat labels cleaned (`'STANDARD_FIBER'`/`'ENERGY_RATING'`/`'CONCIERGE_NODE'`/`'CHARGING_SYNC'` → readable); `grid.kicker` `'REGISTRY_COLLECTION // 2026'` → `'Urban Collection // 2026'`; `grid.title` `'Registry Node Units'` → `'Available Properties'`; `grid.description` removed "synchronized with our global registry node"; `offline.kicker` `'Property Sync Offline'` → `'Connection Error'`; raw `{propertyError}` replaced with static buyer message; `empty.kicker` `'Empty Property Registry'` → `'No Properties Yet'`; `empty.description` removed "backend"/"hydrate automatically"; `cta.kicker` `'CITY_PULSE_PROTOCOL'` → `'City Pulse'`; `cta.description` removed "urban node that matches your frequency"; CTA button converted from `<button onClick={router.push('/')}>` to `<a href={themeLink('/explore')}>` link; `cta.button_label` `'Authorize District Sync'` → `'Explore Properties'`.
- Fixed `SkylineHeader`: added `usePropertyThemeLink` import; logo `<div>` → `<a href={themeLink('/')}>` link; `SYNC_ACTIVE` status badges (mobile + desktop) → `Sellio Platform`.
- Fixed `BrutalistUnitCard`: `V8_NODE` badge → `Verified`.
- Fixed `CityPulseFooter`: converted from arrow expression to block body; added `usePropertyThemeLink`; logo `<div>SKYLINE</div>` → `<a href={themeLink('/')}>SKYLINE</a>` link; description removed "most advanced high-fidelity urban distribution network"; copyright `'© 2026 SELLIO_SKYLINE_OS // VERTICAL_SYNC_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `properties_urban` section: all 29 values updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_urban` HTTP 200.

### `properties_neighborhood` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `Page.tsx`: location fallback `'Neighborhood Node'` → `'Neighborhood'`; `hero.kicker` `'COMMUNITY_RESIDENTIAL_PROTOCOL_V8'` → `'Community Residential'`; `hero.description` removed "property distribution" and "neighborhood nodes"; both hero CTA buttons converted from `<button onClick={router.push}>` to `<a href={themeLink('/explore')}>` links; `hero.primary_cta_label` `'Search_Homes'` → `'Search Homes'`; `hero.secondary_cta_label` `'Local_Guides'` → `'Local Guides'`; `hero.safety_label` `'NEIGHBORHOOD_SAFETY_INDEX'` → `'Safety Index'`; HUD defaults cleaned (`'SCHOOL_NODE_RATING'` → `'School Rating'`, `'A+ SUPERIOR'` → `'A+'`, `'TOP_RATED_SCHOOLS'` → `'Top Schools'`, `'AVG_COMMUTE'` → `'Avg Commute'`, `'18 MIN'` → `'18 min'`, `'COMMUNITY_EVENTS'` → `'Community Events'`, `'42 ACTIVE'` → `'42 Active'`); `grid.kicker` `'RESIDENTIAL_INVENTORY'` → `'Residential Inventory'`; `grid.description` removed "neighborhood protocol" and "synchronized with local lifestyle metadata"; offline state hardcoded `'Property Sync Offline'` → `'Connection Error'`, raw `{propertyError}` replaced with static buyer message; empty state `'Empty Property Registry'` → `'No Properties Yet'`, removed "backend"/"hydrate automatically"; `philosophy.description` "synchronizes with your lifestyle" → "fits your lifestyle"; `philosophy.metric_1_label` `'VERIFIED_LISTINGS'` → `'Verified Listings'`; `philosophy.metric_2_label` `'HOOD_SUPPORT'` → `'Resident Support'`; `join.kicker` `'JOIN_THE_NEIGHBORHOOD'` → `'Join the Neighborhood'`; `join.title` `'Synchronize with \nYour Community.'` → `'Connect with \nYour Community.'`; `join.description` "through your Sellio Hood node" → "through your Sellio account"; `join.button_label` `'CREATE_COMMUNITY_PROFILE'` → `'Create Your Profile'`.
- Fixed `CommunityHeader`: added `usePropertyThemeLink` import; logo `<div className="pn-logo">HOOD<span>Node</span></div>` → `<a href={themeLink('/')}>HOOD<span>Finder</span></a>` link (also renamed "Node" → "Finder").
- Fixed `NeighborPropertyCard`: `VERIFIED_NODE` badge → `Verified`.
- Fixed `HoodFooter`: converted from arrow expression to block body; added `usePropertyThemeLink`; logo `<div className="pn-logo">HOODNODE</div>` → `<a href={themeLink('/')}>HOODFINDER</a>` link; description removed "high-fidelity neighborhood registry" and "Synchronizing local insights with global residential nodes"; copyright `'© 2026 SELLIO_NEIGHBORHOOD_OS // TRUST_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `properties_neighborhood` section: all 30 values updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_neighborhood` HTTP 200.

### `properties_unified` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `Page.tsx`: location fallback `'Global Node'` → `'Location not specified'`; `hero.kicker` `'UNIVERSAL_PROPERTY_PROTOCOL_V8'` → `'Unified Property Platform'`; `hero.title` `'The Authoritative \nDistribution \nNode.'` → `'The Complete \nProperty \nPlatform.'`; `hero.highlight` `'Node.'` → `'Platform.'`; `hero.description` removed "High-fidelity data, institutional-grade verification"; `hero.primary_cta_label` `'Search All Assets'` → `'Browse Listings'`; `hero.secondary_cta_label` `'List Asset'` → `'List a Property'`; secondary hero button converted from `<button onClick={() => window.open(adminCreatePropertyUrl, ...)}>`  to `<a href={adminCreatePropertyUrl} target="_blank" rel="noopener noreferrer">`; `grid.kicker` `'MASTER_REGISTRY'` → `'Property Catalog'`; `grid.title` `'High-Fidelity \nInventory.'` → `'Available \nProperties.'`; `grid.description` removed "unified protocol synchronizes metadata" and "registry node"; `offline.kicker` `'Property Sync Offline'` → `'Connection Error'`; `offline.title` `'High-fidelity inventory could not be loaded.'` → `'Properties could not be loaded.'`; raw `{propertyError}` replaced with static buyer message; `empty.kicker` `'Empty Property Registry'` → `'No Properties Yet'`; `empty.description` removed "backend"/"hydrate automatically"; `cta.kicker` `'INSTITUTIONAL_SCALE'` → `'Grow Your Portfolio'`; `cta.description` removed "unified protocol"/"nodal integrity"; `cta.button_label` `'Initialize Master Node'` → `'Get Started'`; CTA button converted from `<button onClick={() => router.push(themeLink('/'))}>` to `<a href={themeLink('/explore')}>` link.
- Fixed `UniversalHeader`: added `usePropertyThemeLink` import; logo `<div className="uh-logo">` → `<a href={themeLink('/')}>` link; `SYNC_STABLE` status badges (mobile + desktop) → `Sellio Platform`.
- Fixed `MarketMetricsHUD`: labels/values `'ASSETS_MANAGED'` → `'Assets Managed'`; `'NODAL_SYNC'`/`'ACTIVE'` → `'Platform Status'`/`'Active'`; `'VALUATION_ACCURACY'` → `'Listing Accuracy'`; `'COMPLIANCE'`/`'VERIFIED'` → `'Compliance'`/`'Verified'`.
- Fixed `GlobalFooter`: converted from arrow expression to block body; added `usePropertyThemeLink`; logo `<div className="uh-logo">UNIFIED</div>` → `<a href={themeLink('/')}>UNIFIED</a>` link; description removed "most robust universal property protocol" and "Synchronizing...nodes with institutional precision"; copyright `'© 2026 SELLIO_UNIFIED_PROTOCOL // GLOBAL_SYNC_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `properties_unified` section: all 19 values updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/properties_unified` HTTP 200.

### `ecommerce_luxury` - Polished 2026-06-13

Result:

- Fixed `Page.tsx`: `sync.offline_kicker` `'Collection Sync Offline'` → `'Collection Unavailable'`; `empty.description` removed "backend"/"hydrate automatically"; raw `{apiError}` replaced with static buyer message.
- `LuxuryHeader` and `LuxuryFooter` were already clean — logo already uses `<a href={themeLink('/')}>` and footer copyright was already `'© 2026 Aurelia Maison. All Rights Reserved.'`
- Updated ThemeSeeder `ecommerce_luxury` section: `sync.offline_kicker` and `empty.description` values updated.
- Verified: `npm.cmd run lint` (0 errors), `/preview/ecommerce_luxury` HTTP 200.

### `unifieds_mega` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `Page.tsx`: `hero.eyebrow` `'HEAVYWEIGHT_LOGIC_ACTIVE'` → `'Heavyweight Marketplace'`; `hero.description` removed "high-fidelity distribution node"/"Precision structural engineering"; `hero.primary_cta_label` `'INITIALIZE MEGA SYNC'` → `'Browse Marketplace'`; `hero.secondary_cta_label` `'INFRASTRUCTURE SPEC'` → `'View Catalog'`; both hero buttons converted from `<button onClick={router.push}>` to `<a href>` links; `collection.eyebrow` `'LIVE_MEGA_EXCHANGE'` → `'Live Marketplace'`; `collection.description` removed "reinforced inside the Mega Grid catalog layer"; `sync.offline_kicker` `'GRID_OFFLINE'` → `'Connection Error'`; raw `{listingError}` replaced with static buyer message; `empty.kicker` `'EMPTY_GRID'` → `'No Listings Yet'`; `empty.description` removed "backend"/"hydrate automatically"; `mid_section.eyebrow` `'INDUSTRIAL_STRENGTH'` → `'Built for Scale'`; `mid_section.description` removed "Mega Grid protocol"/"every node"/"multi-layer redundancy"; `mid_section.metric_1_label` `'CORE_LATENCY'` → `'Core Latency'`; `mid_section.metric_2_label` `'NODAL_UPTIME'` → `'Uptime'`; `cta.title` `'Authorize\nDistribution.'` → `'Grow Your\nMarketplace.'`; `cta.description` removed "core node"/"high-fidelity distribution network"; `cta.button_label` `'INITIALIZE HEAVYWEIGHT NODE'` → `'Get Started'`; CTA button converted to `<a>` link; product card `MEGA_ID_{id}` → `product.category_title || 'Listing'`; product fallback description removed "synchronized into the Mega Grid exchange"; `'Inspect Node'` → `'View Details'`; floating badge `REINFORCED` → `Certified`.
- Fixed `MegaHeader`: added `useUnifiedThemeLink` import; logo `<div className="ugm-logo">` → `<a href={themeLink('/')}>` link.
- Fixed `HeavyweightGrid`: stat titles cleaned (`"GLOBAL_THROUGHPUT"` → `"Global Throughput"`, `"NODAL_CAPACITY"` → `"Active Capacity"`, `"REGISTRY_VOLUME"` → `"Verified Listings"`, `"SYSTEM_UPTIME"` → `"System Uptime"`); grid header eyebrow `HEAVYWEIGHT_LOGIC` → `Platform Capacity`.
- Fixed `MassiveSyncBar`: replaced all three internal protocol strings with clean buyer-facing copy.
- Fixed `AuthorityFooter`: converted from arrow expression to block body; added `useUnifiedThemeLink`; logo `<div>MEGAGRID</div>` → `<a href={themeLink('/')}>MEGAGRID</a>` link; description cleaned; copyright `'© 2026 SELLIO_MEGAGRID_OS // SYNC_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `unifieds_mega` section: all 25 values updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/unifieds_mega` HTTP 200.

### `unifieds_standard` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `Page.tsx`: `hero.eyebrow` `'MODULAR_DISTRIBUTION_V1'` → `'Standard Marketplace'`; `hero.description` removed "high-fidelity distribution node"; `hero.primary_cta_label` `'INITIALIZE NODE'` → `'Browse Listings'`; `hero.secondary_cta_label` `'VIEW DOCUMENTATION'` → `'View Catalog'`; `layers.title` `'Universal Logic Layers.'` → `'Platform Features.'`; `collection.eyebrow` `'LIVE_EXCHANGE'` → `'Live Listings'`; `collection.description` removed "synchronized into the Scale Protocol"; `sync.offline_kicker` `'PROTOCOL_OFFLINE'` → `'Connection Error'`; raw `{listingError}` replaced with static buyer message; `empty.kicker` `'EMPTY_PROTOCOL_EXCHANGE'` → `'No Listings Yet'`; `empty.description` removed "backend"/"hydrate automatically"; `mid_section.eyebrow` `'GEOMETRIC_PRECISION'` → `'Built for Efficiency'`; `mid_section.description` removed "every node"/"Scale Protocol"/"distribution latency"; `mid_section.metric_1_label` `'AVERAGE_SYNC'` → `'Avg Response'`; `mid_section.metric_2_label` `'ISO_COMPLIANCE'` → `'Compliance'`; `cta.title` `'Initialize the\nStandard.'` → `'Start Selling\nToday.'`; `cta.description` removed "core node"/"high-fidelity distribution network"; `cta.button_label` `'CONNECT SCALE NODE'` → `'Get Started'`; CTA and hero buttons converted to `<a href>` links; product card `SCALE_{id}` → `product.category_title || 'Listing'`; description fallback cleaned; `'Open Node'` → `'View Details'`; price fallback `'Scale quote'` → `'Price on request'`.
- Fixed `ScaleHeader`: added `useUnifiedThemeLink` import; logo `<div className="usp-logo">` → `<a href={themeLink('/')}>` link.
- Fixed `ProtocolGrid`: all six feature card tags cleaned from underscore protocol names (`DATA_LAYER`, `SYNC_ENGINE`, `UI_PROTOCOL`, `NODE_LOGIC`, `AUTH_LAYER`, `CORE_API`) to readable labels; descriptions updated to remove "institutional-grade redundancy", "high-fidelity", "sovereign architectural silos", "zero-dependency", "distribution nodes".
- Fixed `EfficiencyBar`: replaced three internal protocol strings with clean buyer-facing copy.
- Fixed `StandardFooter`: converted from arrow expression to block body; added `useUnifiedThemeLink`; logo `<div>SCALEPROTOCOL</div>` → `<a href={themeLink('/')}>SCALEPROTOCOL</a>` link; description cleaned; copyright `'© 2026 SELLIO_SCALEPROTOCOL_OS // STANDARDS_ACTIVE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `unifieds_standard` section: all 27 values updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/unifieds_standard` HTTP 200.

### `unifieds_classic` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `Page.tsx`: `hero.eyebrow` `'TRADITION_OF_EXCELLENCE'` → `'Classic Marketplace'`; `hero.title` `"The Heritage of\nDistribution."` → `"The Heritage of\nCommerce."`; `hero.description` removed "high-fidelity foundational node"; `hero.primary_cta_label` `'ENTER THE ARCHIVE'` → `'Browse Catalog'`; `hero.secondary_cta_label` `'READ THE CHRONICLES'` → `'View Listings'`; both hero buttons converted from `<button onClick={router.push}>` to `<a href={themeLink('/explore')}>` links; `collection.eyebrow` `'LIVE_HERITAGE_REGISTRY'` → `'Live Catalog'`; `collection.description` removed "Legacy Registry"; `sync.offline_kicker` `'ARCHIVE_OFFLINE'` → `'Connection Error'`; `sync.offline_title` cleaned; `empty.kicker` `'EMPTY_ARCHIVE'` → `'No Listings Yet'`; `empty.description` removed "backend"/"hydrate automatically"; `mid_section.eyebrow` `'TIME_HONORED_PRECISION'` → `'Time-Honored Quality'`; `mid_section.description` removed "Legacy Node protocol"/"distribution logic"/"high-fidelity assets"/"global network"; `mid_section.metric_1_label` `'CORE_LOGIC_AGE'` → `'Years of Excellence'`; `mid_section.metric_2_label` `'ASSET_PROVENANCE'` → `'Verified Listings'`; `cta.description` removed "core node"/"Legacy Registry"/"high-fidelity distribution network"; `cta.button_label` `'CONNECT LEGACY NODE'` → `'Get Started'`; CTA button converted to `<a href>` link; product card `ARCHIVE_{id}` → `product.category_title || 'Listing'`; `'View Provenance'` → `'View Details'`; price fallback `'Inquire for legacy rate'` → `'Price on request'`.
- Fixed `LegacyHeader`: added `useUnifiedThemeLink` import; logo `<div className="uc-logo">` → `<a href={themeLink('/')}>` link.
- Fixed `HeritageGrid`: `"ESTABLISHED_1996"` → `"Established 1996"`; all three card descriptions cleaned of "high-fidelity foundation", "Legacy Registry", "Synchronizing global commerce nodes", "structural protocol".
- Fixed `ChronicleBar`: replaced three internal protocol strings (`CORE LEGACY HANDSHAKE PROTOCOL ENFORCED`, `PROVENANCE TRUST METRIC: 100% SECURE INTEGRATION`, `VERIFIABLE ARCHIVE STABILITY: v8.4.1`) with clean buyer-facing copy.
- Fixed `AncestralFooter`: converted from arrow expression to block body; added `useUnifiedThemeLink`; logo `<div>THELEGACYNODE</div>` → `<a href={themeLink('/')}>THELEGACYNODE</a>` link; description removed "high-fidelity foundational registry"/"modern secure protocols"; copyright `'© 2026 SELLIO_LEGACY_OS // HORIZON_SYNC_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `unifieds_classic` section: all 25 values updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/unifieds_classic` HTTP 200.

### `unifieds_interactive` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `Page.tsx`: `hero.eyebrow` `'KINETIC_TRANSMISSION_V4'` → `'Interactive Marketplace'`; `hero.description` removed "high-fidelity interaction node"/"Synchronize your digital distribution"; `hero.primary_cta_label` `'INITIALIZE SYNC'` → `'Browse Listings'`; `hero.secondary_cta_label` `'READ THE DYNAMICS'` → `'View Catalog'`; both hero buttons converted from `<button onClick={router.push}>` to `<a href>` links; `collection.eyebrow` `'LIVE_MOTION_FEED'` → `'Live Listings'`; `collection.description` removed "Motion Node catalog"; `sync.offline_kicker` `'MOTION_OFFLINE'` → `'Connection Error'`; `sync.offline_title` changed to "Listings could not be loaded."; `empty.kicker` `'EMPTY_MOTION_FEED'` → `'No Listings Yet'`; `empty.description` removed "backend"/"hydrate automatically"; `mid_section.description` removed "Every interaction is a node"/"high-fidelity protocol"; `cta.title` `"Ready to\nTransition?"` → `"Ready to\nGet Started?"`; `cta.description` removed "interaction node"/"high-fidelity distribution network"; `cta.button_label` `'CONNECT MOTION NODE'` → `'Get Started'`; CTA button converted to `<a>` link; mid-section feature list cleaned (`'Dynamic Schema Fluids'` → `'Dynamic Product Categories'`, `'Kinetic Asset Mapping'` → `'Fast Asset Delivery'`, `'Real-time Interaction Sync'` → `'Real-Time Listing Updates'`); raw `{listingError}` replaced with static buyer message; product card `MOTION_ID_{id}` → `product.category_title || 'Listing'`; description fallback cleaned; `'Open Motion'` → `'View Details'`; price fallback `'Sync quote'` → `'Price on request'`.
- Fixed `MotionHeader`: added `useUnifiedThemeLink` import; logo `<div className="ui-logo">MOTIONNODE</div>` → `<a href={themeLink('/')}>MOTIONHUB</a>` link.
- Fixed `InteractionCanvas`: `'DYNAMIC_SCHEMA_V4'` → `'Interactive Platform'`; description cleaned; `'FLUID_LATENCY'` → `'Fluid Transitions'`; `'INTERACTION_SYNC'` → `'Live Updates'`; `'Nodes_Active'` → `'Channels Active'`; `'CLICK TO TOGGLE ACTIVE FLOWS'` → `'Click to toggle channels'`.
- Fixed `FluidLogicBar`: replaced three internal protocol strings with clean buyer-facing copy.
- Fixed `KineticFooter`: converted from arrow expression to block body; added `useUnifiedThemeLink`; logo `<div>MOTIONNODE</div>` → `<a href={themeLink('/')}>MOTIONHUB</a>` link; description cleaned; copyright `'© 2026 SELLIO_MOTION_OS // INSTANT_SYNC_STABLE'` → `'© 2026 Sellio. All rights reserved.'`
- Updated ThemeSeeder `unifieds_interactive` section: all 20 values updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/unifieds_interactive` HTTP 200.

### `classifieds_deals` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `handleCardClick`: `router.push(themeLink(...))` → `window.location.href = themeLink(...)` (DealCard has nested `<button>` children — converting outer div to `<a>` would produce invalid HTML; kept `<div onClick>` pattern).
- Fixed hero carousel CTA: `<button onClick={() => router.push(...)}>` → `<a href={themeLink('/explore')}>` link.
- Fixed `DealsHeader`: added `useClassifiedsThemeLink`; logo `href="#"` → `href={themeLink('/')}`.
- Fixed `DealsFooter`: added `useClassifiedsThemeLink`; footer logo `href="#"` → `href={themeLink('/')}`; description removed "high-velocity"; copyright removed "Engineered for Envato Elite Performance".
- Fixed ThemeSeeder `classifieds_deals` block: removed stale `diagnostics` row; corrected all content_key naming from underscore to dot notation (`slide_1_title` → `slide_1.title`, `flash_sale_title` → `flash_sale.title`, `ad_badge` → `ad.badge`) so merchant customization resolves correctly via `useThemeContent('hero.slide_1.title', ...)`.
- Verified: `npm.cmd run lint` (0 errors), `/preview/classifieds_deals` HTTP 200.

### `classifieds_elite` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `PremiumCard onClick`: `router.push(themeLink(...))` → `window.location.href = themeLink(...)` (PremiumCard has nested `<button>` children — kept outer `<div onClick>` to avoid invalid HTML nesting).
- Fixed hero subtitle default: `'Vetted Global Advisory Node'` → `'Elite Curated Classifieds'`.
- Fixed `prospectusButton` default: `'Request Prospectus memorandum'` → `'View Listing Details'`.
- Fixed `inquiryButton` default: `'Inquire Concierge Vault'` → `'Submit Inquiry'`.
- Fixed loading copy: `'Evaluating ledger...'` → `'Loading listings...'`; result count `'ultra-curated assets'` → `'listings'`.
- Fixed `getAssetVaultId` fallback: `VAULT_${city}_${id}` → `#${item.id}`; label `'Vault ID'` → `'Listing ID'`.
- Fixed tooltip: `'Interactive Terminal node'` → `'Share listing'`.
- Rewrote `EliteHeader.tsx`: logo `<div>` → `<a href={themeLink('/')}>` using `useClassifiedsThemeLink`; removed dead `href="#"` and window.location.reload() on logo click.
- Rewrote `DiamondFooter.tsx`: logo `<div>` → `<a href={themeLink('/')}>` link; description cleaned of "certified vaults"; copyright `'© 2026 SELLIO_ELITE_HOLDINGS. ALL RIGHTS RESERVED.'` → `'© 2026 Sellio. All rights reserved.'`; `'PRIVACY_PROTECTED_NODE'` → `'🔒 Secure & Verified Marketplace'`.
- Fixed `luxury-components.tsx` (`PremiumHeader`, `PremiumFooter`): added `useClassifiedsThemeLink`; logo `href="#"` → `href={themeLink('/')}`; description cleaned; copyright normalized; encrypted vault text → `'🔒 Secure & Verified Marketplace'`. (These are exported for compatibility; active layout uses `EliteHeader`/`DiamondFooter`.)
- Updated ThemeSeeder `classifieds_elite` block: removed two stale `diagnostics` rows; updated `hero.subtitle`, `hero.title`, `quickview.prospectus_button`, `quickview.inquiry_button` to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/classifieds_elite` HTTP 200.

### `unifieds_minimal` - Polished 2026-06-13

Result:

- Fixed `Page.tsx`: `hero.eyebrow` `'UNIVERSAL MINIMALISM'` → `'Minimal Marketplace'`; raw `{listingError}` replaced with static buyer message; empty state description removed "backend"/"hydrate automatically"; category empty state removed "catalog registry".
- Fixed `ZenFooter`: converted from arrow expression to block body; added `useUnifiedThemeLink`; logo `<div className="usm-logo">` → `<a href={themeLink('/')}>` link; description cleaned; copyright `'© 2026 Universal Marketplace. All rights reserved.'` → `'© 2026 Sellio. All rights reserved.'`
- Fixed `SilentHeader` modal: `'SELLER CONSOLE'` → `'Sell on Sellio'`; success message removed "catalog registry".
- Updated ThemeSeeder `unifieds_minimal` section: `hero.eyebrow` updated.
- Verified: `npm.cmd run lint` (0 errors), `/preview/unifieds_minimal` HTTP 200.

### `jobs_startup` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `Page.tsx`: `hero.eyebrow` `'SYNCHRONIZE_TALENT_V5'` → `'Startup Jobs'`; `hero.description` removed "high-fidelity distribution node"/"career node"; `hero.primary_cta_label` `'EXPLORE_VENTURES'` → `'Browse Jobs'`; `hero.secondary_cta_label` `'VENTURE_CAPITAL_ACCESS'` → `'View Opportunities'`; trust bar `'VENTURE_FUNDING_SYNC: ACTIVE'` → `'Funded Startups'`, `'EQUITY_VERIFIED: TRUE'` → `'Equity Verified'`, `'NETWORK_NODE: 5.0_ELITE'` → `'Top-Rated Network'`; removed `LATENCY: {loading}` diagnostic display → `'New Roles Daily'`; stat labels `'VERIFIED_STARTUPS'` → `'Verified Startups'`, `'TOTAL_EQUITY_VALUE'` → `'Total Equity'`, `'NODAL_CONNECTIONS'` → `'Connections Made'`; `cta.description` removed "Initialize your professional growth node"/"high-fidelity equity structures"; `cta.button_label` `'INITIALIZE_GROWTH_NODE'` → `'Browse Opportunities'`; both hero buttons and CTA button converted from `<button onClick>` to `<a href={themeLink('/explore')}>` links.
- Fixed `GrowthHeader.tsx`: added `useJobsThemeLink`; `brand_label` default `'GROWTH_NODE.'` → `'Venture Hub.'`; logo converted from dead `<div>` to `<div onClick={() => window.location.href = themeLink('/')} style={{ cursor: 'pointer' }}>` (Note: `<a>` was attempted but caused hydration crash because the inner `<div>` icon creates block-in-inline nesting; `onClick` pattern used instead).
- Fixed `NetworkFooter.tsx`: added `useJobsThemeLink`; logo `<div className="growth-logo">` → `<a href={themeLink('/')}>` link (text-only content, no inner block element); description removed "high-fidelity startup distribution node"/"Synchronizing talent"; copyright `'(c) 2026 GROWTH_NODE_SYSTEMS. ALL_SYSTEMS_GO.'` → `'© 2026 Sellio. All rights reserved.'`; version label default `'v.4.2_ELITE_VC'` → `''` (hidden when empty).
- Fixed `MissionControlSection.tsx`: `mission.eyebrow` `'MISSION_CONTROL'` → `'Our Mission'`; description removed "every startup node"/"mission-readiness"/"team logic"/"highest-fidelity"; `'TOTAL_VC_LIQUIDITY'` → `'VC Funding Tracked'`; `'AVG_EQUITY_POOL'` → `'Avg. Equity Offered'`.
- Fixed `OpportunityGrid.tsx`: empty state "backend" → "admin panel".
- Updated ThemeSeeder `jobs_startup` section: all 29 values updated to match cleaned defaults.
- Verified: `npm.cmd run lint` (0 errors), `/preview/jobs_startup` HTTP 200.
- Also fixed pre-existing TypeScript error across 4 unifieds themes (`unifieds_classic`, `unifieds_interactive`, `unifieds_mega`, `unifieds_standard`): `product.category_title` (field does not exist on `Product` type) → `'Listing'`.
- Also fixed pre-existing SSR crash in `ThemeSwitcherClient.tsx` (from last committed change): `window.innerHeight` in `useState` initializer → guarded with `typeof window !== 'undefined'` check.

### `jobs_freelance` - Polished 2026-06-13

Result:

- Removed unused `useRouter` import and `const router = useRouter();`.
- Fixed `Page.tsx`: search input `onFocus={goToExplore}` → `onFocus={() => { window.location.href = themeLink('/explore'); }}`; search button `<button onClick>` → `<a href={themeLink('/explore')} className="jf-btn jf-btn-primary">`; all 8 category pill `<button onClick>` → `<a href={themeLink('/explore')} className="jf-cat-pill">`; empty state kicker `'Empty Gig Registry'` → `'No Gigs Yet'`; empty state description removed "backend"/"hydrate"; empty state button `<button onClick>` → `<a href>`; promo button `<button onClick>` → `<a href={themeLink('/explore')} className="jf-btn">`.
- Fixed `components/index.tsx` (`FreelanceHeader`): added `useJobsThemeLink`; logo `href="#"` → `href={themeLink('/')}`.
- Fixed `components/index.tsx` (`FreelanceFooter`): converted from arrow expression to block body; added `useJobsThemeLink`; logo `href="#"` → `href={themeLink('/')}`; copyright `'© 2026 GigHive International Ltd.'` → `'© 2026 GigHive. All rights reserved.'`.
- ThemeSeeder `jobs_freelance` section: already clean — no changes needed.
- Verified: `npm.cmd run lint` (0 errors), `/preview/jobs_freelance` HTTP 200.
