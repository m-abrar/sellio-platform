# SELLIO PLATFORM - TODO LIST

## 🔴 CRITICAL FIXES
- [/] **Fix SettingController Crash:** Resolve `compact(): Undefined variable $themes` error.
  * *Status:* Variable initialized in `SettingController@getSection`, but needs verification that `admin/settings/group/modules` loads correctly.
- [/] **Frontend Navigation:** Resolve why the menu is not showing on the storefront.
  * *Status:* Pending investigation of `apps/web` menu components.

## 🎨 THEME MANAGER (ADMIN)
- [ ] **Library Visibility:** Show all themes in the "Themes Library" tab.
  * *Context:* Currently, themes in "Recently Used" are filtered out of the library.
- [ ] **New "Ecommerce" Category:** Add a dedicated "Shop" tab in the Theme library.
- [ ] **Theme Sub-niches:** Categorize eCommerce themes by industry:
  * Sports, Clothing, Medical, Bakery, Dental Instruments, etc.

## 🌐 FRONTEND UX & SETTINGS
- [ ] **Logo vs Text Toggle:** Provide a backend setting to hide "Company Name" text if a "Logo Image" is present in the header.
  * *Context:* Avoid irrelevant duplication in the header.
- [ ] **Dashboard Access:** Add a button/link in the Admin Dashboard to quickly "Open Frontend".
can you do it for the laravel blades first? then the nextjs frontend later.

## 🏗️ MONOREPO & DEVOPS
- [x] **Monorepo Migration:** Apps moved to `apps/` (backend, web, dashboard, mobile).
- [x] **File Management:** Moved `TODO.txt` to the root directory.
- [ ] **Git Configuration:** Update `.gitignore` for the new structure and ignore `TODO.txt`.

------------------

when mysql server is down, we see this error, can you show a professional polished screen?

Illuminate\Database\QueryException
vendor\laravel\framework\src\Illuminate\Database\Connection.php:826
SQLSTATE[HY000] [2002] No connection could be made because the target machine actively refused it (Connection: mysql, SQL: select `value` from `settings` where `key` = site_name limit 1)

----------------------

http://127.0.0.1:8000/admin/system/maintenance
i want to see success / error alerts on this page


-------------------------

Do we have dedicated blade/view and controllers for each module?
why are you on the common module/controller?

http://127.0.0.1:8000/admin/listings/auto/30

Error
vendor\laravel\framework\src\Illuminate\Routing\ControllerDispatcher.php:46
Call to undefined method App\Http\Controllers\Admin\ListingController::edit()

-----------------------------

Please check why fallback image is not showing in the admin dashboard for most of the pages?

$model->thumbnail_url

From the HasImageAccess.php trait, we have
public function getThumbnailUrlAttribute(): string
    {
        return $this->getImageUrl(conversion: 'thumb');
    }

getFallbackImage()

-----------------------------

Do you have separate folders for each nextjs app in this monorepo?

--------------------------