# SELLIO PLATFORM - MODERN TODO

## 🔴 CRITICAL: IMAGE SYSTEM
- [x] **Fix Fallback Images:** Images not showing in Admin Dashboard for most pages.
  * *Status:* Resolved by converting to root-relative paths in `HasImageAccess.php`.

## 🟠 BACKEND STABILITY
- [x] **Reset SettingController:** Ensure `compact(): Undefined variable $themes` is fully resolved.
  * *Status:* Patched with robust initialization in Phase 1.
- [x] **Header Configuration:**
  * Add `hide_site_name` toggle in Backend Settings (General).
  * Update validation rules in `SettingController`.
  * Add UI switch in `general.blade.php`.

## 🟡 FRONTEND & STOREFRONT (apps/web)
- [x] **Navigation Restoration:**
  * Create `Header.tsx` component with dynamic branding logic.
  * Integrate Header into `RootLayout`.
  * Restore Primary Navigation Menu (Marketplace, Products, Real Estate, etc.).

## 🔵 THEME MANAGER
- [x] **Library Optimization:**
  * Remove "Recently Used" exclusion from the Theme Library.
  * Add dedicated "Ecommerce/Shop" categories and industry sub-niches with branding icons.

## 🟢 MONOREPO HYGIENE
- [x] **Git Cleanup:** Update root `.gitignore` and ignore `TODO.txt`.
- [x] **Logistics:** Moved `TODO.txt` to root.


----------------------


Admin Images -> Fallbacks now load using relative paths.
NO, can you make it absolute? in the trait getFallbackImage()



----------------------


when mysql server is down, we see this error, can you show a professional polished screen?

Illuminate\Database\QueryException
vendor\laravel\framework\src\Illuminate\Database\Connection.php:826
SQLSTATE[HY000] [2002] No connection could be made because the target machine actively refused it (Connection: mysql, SQL: select `value` from `settings` where `key` = site_name limit 1)

----------------------

http://127.0.0.1:8000/admin/system/maintenance
i want to see success / error alerts on this page


-------------------------

Do we have dedicated blade/view and controllers for each module?
why are you on the common module/controller?

http://127.0.0.1:8000/admin/listings/auto/30

Error
vendor\laravel\framework\src\Illuminate\Routing\ControllerDispatcher.php:46
Call to undefined method App\Http\Controllers\Admin\ListingController::edit()


-----------------------------

Do you have separate folders for each nextjs app in this monorepo?

--------------------------