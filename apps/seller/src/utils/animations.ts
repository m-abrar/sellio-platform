import { motion } from 'motion/react';

export const triggerCelebration = async () => {
  console.log('Celebration triggered!');
  // In a real app, this might trigger a confetti effect or a specific animation
};

export const triggerDeletion = () => {
  console.log('Deletion animation triggered!');
};
